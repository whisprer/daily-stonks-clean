[pytest]
testpaths = tests
addopts = -q -m "not integration"
python_files = test_*.py
markers =
    integration: tests that require docker/postgres and run slower
filterwarnings =
    ignore:.*collation version.*:Warning