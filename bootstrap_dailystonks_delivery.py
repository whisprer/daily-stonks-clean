#!/usr/bin/env python3
"""
Generate a minimal DailyStonks delivery layer (FastAPI + Postgres + cron runner).

Usage:
  python bootstrap_dailystonks_delivery.py [target_dir] [--force]

It will create the project in target_dir (default: dailystonks-delivery).
"""

from __future__ import annotations

import sys
from pathlib import Path

FILES = {
  "README.md": "# DailyStonks Delivery Service (cards \u2192 preferences \u2192 scheduled email)\n\nThis repo is a **minimal, working** delivery layer that:\n- exposes a card catalog\n- hosts a simple preferences page (checkbox selection + schedule time)\n- runs scheduled deliveries via a tiny runner you can call from cron\n- accepts PayPal webhooks to upgrade/downgrade tiers (optional signature verification)\n\n## Quick start (local)\n\n```bash\n# 1) Start Postgres\ndocker compose up -d\n\n# 2) Create venv + deps\npython -m venv .venv\n. .venv/bin/activate  # Windows: .venv\\\\Scripts\\\\activate\npip install -r requirements.txt\n\n# 3) Configure env\ncp .env.example .env\n# edit .env (SECRET_KEY, SMTP, PUBLIC_BASE_URL, etc.)\n\n# 4) Create tables\npython scripts/init_db.py\n\n# 5) Run API\nuvicorn app.main:app --reload --port 8000\n```\n\n### Create a test user\n\n```bash\n# Using SECRET_KEY or ADMIN_TOKEN as x-admin-token:\ncurl -X POST http://localhost:8000/admin/create-user \\\\\n  -H \"x-admin-token: <ADMIN_TOKEN_OR_SECRET_KEY>\" \\\\\n  -F \"email=you@example.com\" \\\\\n  -F \"tier=PRO\"\n```\n\nOpen the returned `prefs_link`, click **Send test email now** (requires SMTP).\n\n## Scheduled delivery\n\nSet a cron job (every 5 minutes is fine):\n\n```cron\n*/5 * * * * cd /path/to/repo && /path/to/repo/.venv/bin/python scripts/run_due_deliveries.py >> logs/delivery.log 2>&1\n```\n\n## Plug in your real cards and data\n\n- Replace `app/cards/registry.py` to load **your existing card modules**.\n- Replace `app/data/provider.py` so `load_payload(asof_date)` returns the bundle your cards expect.\n- Ensure your site hosts charts under `${PUBLIC_BASE_URL}/charts/YYYY-MM-DD/...`.\n\n## PayPal tiers\n\nSet `PAYPAL_PLAN_TIER_MAP` in `.env` to map your PayPal plan IDs to tiers.\nIf you set `PAYPAL_WEBHOOK_ID`, `PAYPAL_CLIENT_ID`, and `PAYPAL_CLIENT_SECRET`,\nthe webhook receiver will attempt to verify signatures.\n\nEndpoint: `POST /webhooks/paypal`\n",
  "requirements.txt": "fastapi>=0.110\nuvicorn[standard]>=0.27\nSQLAlchemy>=2.0\npsycopg2-binary>=2.9\npydantic>=2.5\njinja2>=3.1\npython-dotenv>=1.0\nhttpx>=0.27\nemail-validator>=2.1\n",
  "docker-compose.yml": "version: \"3.9\"\nservices:\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_PASSWORD: postgres\n      POSTGRES_USER: postgres\n      POSTGRES_DB: dailystonks\n    ports:\n      - \"5432:5432\"\n    volumes:\n      - ds_pg:/var/lib/postgresql/data\nvolumes:\n  ds_pg: {}\n",
  ".env.example": "# Database\nDATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/dailystonks\n\n# Public URL where charts/assets live and where users will open prefs/unsubscribe\nPUBLIC_BASE_URL=http://localhost:8000\n\n# SMTP (use your existing mail relay or provider)\nSMTP_HOST=localhost\nSMTP_PORT=25\nSMTP_USE_TLS=0\nSMTP_USERNAME=\nSMTP_PASSWORD=\nMAIL_FROM=DailyStonks <no-reply@dailystonks.org>\n\n# Secrets\nSECRET_KEY=change-me-to-a-long-random-string\nADMIN_TOKEN=change-me-or-remove-to-use-SECRET_KEY\n\n# Optional PayPal webhook verification\nPAYPAL_WEBHOOK_ID=\nPAYPAL_CLIENT_ID=\nPAYPAL_CLIENT_SECRET=\nPAYPAL_API_BASE=https://api-m.paypal.com\n\n# Plan -> tier mapping (JSON)\nPAYPAL_PLAN_TIER_MAP={\"P-PLANID1\":\"BASIC\",\"P-PLANID2\":\"PRO\",\"P-PLANID3\":\"CUSTOM\"}\n\n# Where your daily bundles live\nDAILYSTONKS_DATA_DIR=./sample_data\n\n# Tier policy file\nTIER_POLICY_PATH=./config/tiers.json\n",
  "config/tiers.json": "{\n  \"tiers\": {\n    \"FREE\": {\n      \"can_customize\": false,\n      \"max_cards\": 6,\n      \"allowed_cards\": [\n        \"sp500_summary\",\n        \"top_movers\",\n        \"heatmap\"\n      ]\n    },\n    \"BASIC\": {\n      \"can_customize\": false,\n      \"max_cards\": 8,\n      \"allowed_cards\": [\n        \"sp500_summary\",\n        \"top_movers\",\n        \"heatmap\",\n        \"sector_perf\",\n        \"news_brief\"\n      ]\n    },\n    \"PRO\": {\n      \"can_customize\": true,\n      \"max_cards\": 12,\n      \"allowed_cards\": [\n        \"*\"\n      ]\n    },\n    \"CUSTOM\": {\n      \"can_customize\": true,\n      \"max_cards\": 25,\n      \"allowed_cards\": [\n        \"*\"\n      ]\n    }\n  }\n}\n",
  "scripts/init_db.py": "from __future__ import annotations\nfrom app.db import engine, Base\nfrom app import models  # noqa: F401\n\ndef main() -> None:\n    Base.metadata.create_all(bind=engine)\n    print(\"DB tables created.\")\n\nif __name__ == \"__main__\":\n    main()\n",
  "scripts/run_due_deliveries.py": "from __future__ import annotations\nfrom app.delivery.runner import run_due_deliveries\n\ndef main() -> None:\n    n = run_due_deliveries(limit=50)\n    print(f\"Processed {n} schedule(s).\")\n\nif __name__ == \"__main__\":\n    main()\n",
  "app/__init__.py": "",
  "app/config.py": "from __future__ import annotations\n\nimport os\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom dotenv import load_dotenv\n\nload_dotenv()\n\ndef _env(name: str, default: str | None = None) -> str | None:\n    v = os.getenv(name)\n    return v if v not in (None, \"\") else default\n\n@dataclass(frozen=True)\nclass Settings:\n    database_url: str\n    public_base_url: str\n    smtp_host: str\n    smtp_port: int\n    smtp_username: str | None\n    smtp_password: str | None\n    smtp_use_tls: bool\n    mail_from: str\n    secret_key: str\n    paypal_webhook_id: str | None\n    paypal_client_id: str | None\n    paypal_client_secret: str | None\n    paypal_api_base: str\n    tier_policy_path: Path\n\ndef get_settings() -> Settings:\n    # Defaults assume docker-compose from this repo.\n    return Settings(\n        database_url=_env(\"DATABASE_URL\", \"postgresql+psycopg2://postgres:postgres@localhost:5432/dailystonks\") or \"\",\n        public_base_url=_env(\"PUBLIC_BASE_URL\", \"http://localhost:8000\") or \"\",\n        smtp_host=_env(\"SMTP_HOST\", \"localhost\") or \"\",\n        smtp_port=int(_env(\"SMTP_PORT\", \"25\") or \"25\"),\n        smtp_username=_env(\"SMTP_USERNAME\"),\n        smtp_password=_env(\"SMTP_PASSWORD\"),\n        smtp_use_tls=(_env(\"SMTP_USE_TLS\", \"0\") == \"1\"),\n        mail_from=_env(\"MAIL_FROM\", \"DailyStonks <no-reply@dailystonks.org>\") or \"\",\n        secret_key=_env(\"SECRET_KEY\", \"dev-secret-change-me\") or \"\",\n        paypal_webhook_id=_env(\"PAYPAL_WEBHOOK_ID\"),\n        paypal_client_id=_env(\"PAYPAL_CLIENT_ID\"),\n        paypal_client_secret=_env(\"PAYPAL_CLIENT_SECRET\"),\n        paypal_api_base=_env(\"PAYPAL_API_BASE\", \"https://api-m.paypal.com\") or \"\",\n        tier_policy_path=Path(_env(\"TIER_POLICY_PATH\", str(Path(__file__).resolve().parent.parent / \"config\" / \"tiers.json\")) or \"\"),\n    )\n",
  "app/db.py": "from __future__ import annotations\n\nfrom contextlib import contextmanager\nfrom sqlalchemy import create_engine\nfrom sqlalchemy.orm import sessionmaker, DeclarativeBase\nfrom .config import get_settings\n\nclass Base(DeclarativeBase):\n    pass\n\n_settings = get_settings()\nengine = create_engine(_settings.database_url, pool_pre_ping=True)\nSessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)\n\n@contextmanager\ndef session_scope():\n    session = SessionLocal()\n    try:\n        yield session\n        session.commit()\n    except Exception:\n        session.rollback()\n        raise\n    finally:\n        session.close()\n",
  "app/models.py": "from __future__ import annotations\n\nimport uuid\nfrom datetime import datetime\nfrom sqlalchemy import (\n    String, DateTime, Boolean, ForeignKey, Integer, Text, UniqueConstraint\n)\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import Mapped, mapped_column, relationship\nfrom .db import Base\n\ndef _utcnow() -> datetime:\n    return datetime.utcnow()\n\nclass User(Base):\n    __tablename__ = \"users\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    email: Mapped[str] = mapped_column(String(320), unique=True, index=True)\n    tier: Mapped[str] = mapped_column(String(32), default=\"FREE\", index=True)\n    is_active: Mapped[bool] = mapped_column(Boolean, default=True)\n    preference_token: Mapped[str] = mapped_column(String(64), unique=True, index=True)\n    created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n\n    targets: Mapped[list[\"DeliveryTarget\"]] = relationship(back_populates=\"user\", cascade=\"all, delete-orphan\")\n    selections: Mapped[list[\"CardSelection\"]] = relationship(back_populates=\"user\", cascade=\"all, delete-orphan\")\n    schedules: Mapped[list[\"DeliverySchedule\"]] = relationship(back_populates=\"user\", cascade=\"all, delete-orphan\")\n    entitlements: Mapped[list[\"Entitlement\"]] = relationship(back_populates=\"user\", cascade=\"all, delete-orphan\")\n\nclass DeliveryTarget(Base):\n    __tablename__ = \"delivery_targets\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"users.id\", ondelete=\"CASCADE\"), index=True)\n    email: Mapped[str] = mapped_column(String(320), index=True)\n    label: Mapped[str] = mapped_column(String(64), default=\"primary\")\n    is_primary: Mapped[bool] = mapped_column(Boolean, default=False)\n    unsubscribed_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)\n    created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n\n    user: Mapped[User] = relationship(back_populates=\"targets\")\n    schedules: Mapped[list[\"DeliverySchedule\"]] = relationship(back_populates=\"target\", cascade=\"all, delete-orphan\")\n\n    __table_args__ = (UniqueConstraint(\"user_id\", \"email\", name=\"uq_target_user_email\"),)\n\nclass CardSelection(Base):\n    __tablename__ = \"card_selections\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"users.id\", ondelete=\"CASCADE\"), index=True)\n    card_id: Mapped[str] = mapped_column(String(64), index=True)\n    enabled: Mapped[bool] = mapped_column(Boolean, default=True)\n    position: Mapped[int] = mapped_column(Integer, default=0)\n    created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n\n    user: Mapped[User] = relationship(back_populates=\"selections\")\n\n    __table_args__ = (UniqueConstraint(\"user_id\", \"card_id\", name=\"uq_selection_user_card\"),)\n\nclass DeliverySchedule(Base):\n    __tablename__ = \"delivery_schedules\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"users.id\", ondelete=\"CASCADE\"), index=True)\n    target_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"delivery_targets.id\", ondelete=\"CASCADE\"), index=True)\n\n    timezone: Mapped[str] = mapped_column(String(64), default=\"Europe/London\")\n    send_time_hhmm: Mapped[str] = mapped_column(String(5), default=\"06:30\")  # local time, 24h\n    enabled: Mapped[bool] = mapped_column(Boolean, default=True)\n    next_run_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow, index=True)\n    created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n\n    user: Mapped[User] = relationship(back_populates=\"schedules\")\n    target: Mapped[DeliveryTarget] = relationship(back_populates=\"schedules\")\n    runs: Mapped[list[\"DeliveryRun\"]] = relationship(back_populates=\"schedule\", cascade=\"all, delete-orphan\")\n\nclass DeliveryRun(Base):\n    __tablename__ = \"delivery_runs\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    schedule_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"delivery_schedules.id\", ondelete=\"CASCADE\"), index=True)\n    run_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow, index=True)\n    status: Mapped[str] = mapped_column(String(16), default=\"PENDING\", index=True)  # PENDING|SENT|FAILED|SKIPPED\n    error: Mapped[str | None] = mapped_column(Text, nullable=True)\n    message_id: Mapped[str | None] = mapped_column(String(255), nullable=True)\n    created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n\n    schedule: Mapped[DeliverySchedule] = relationship(back_populates=\"runs\")\n\n    __table_args__ = (UniqueConstraint(\"schedule_id\", \"run_at\", name=\"uq_run_schedule_runat\"),)\n\nclass PaymentEvent(Base):\n    __tablename__ = \"payment_events\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    provider: Mapped[str] = mapped_column(String(16), default=\"PAYPAL\", index=True)\n    event_id: Mapped[str] = mapped_column(String(128), unique=True, index=True)\n    event_type: Mapped[str] = mapped_column(String(128), index=True)\n    received_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n    processed_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)\n    raw_json: Mapped[str] = mapped_column(Text)\n\nclass Entitlement(Base):\n    __tablename__ = \"entitlements\"\n\n    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)\n    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey(\"users.id\", ondelete=\"CASCADE\"), index=True)\n    tier: Mapped[str] = mapped_column(String(32), index=True)\n    status: Mapped[str] = mapped_column(String(16), default=\"ACTIVE\", index=True)  # ACTIVE|CANCELLED|EXPIRED\n    valid_from: Mapped[datetime] = mapped_column(DateTime, default=_utcnow)\n    valid_to: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)\n    source: Mapped[str] = mapped_column(String(32), default=\"PAYPAL\")\n\n    user: Mapped[User] = relationship(back_populates=\"entitlements\")\n\n    __table_args__ = (UniqueConstraint(\"user_id\", \"tier\", \"status\", \"valid_from\", name=\"uq_entitlement_user_tier_status_from\"),)\n",
  "app/tier.py": "from __future__ import annotations\n\nimport json\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Dict, List, Set\n\nfrom .config import Settings, get_settings\nfrom .cards.base import CardMeta\n\n@dataclass(frozen=True)\nclass TierPolicy:\n    can_customize: bool\n    max_cards: int\n    allowed_cards: List[str]  # list of ids or [\"*\"]\n\ndef load_tier_policies(settings: Settings | None = None) -> Dict[str, TierPolicy]:\n    settings = settings or get_settings()\n    data = json.loads(Path(settings.tier_policy_path).read_text(encoding=\"utf-8\"))\n    tiers = {}\n    for tier, cfg in data.get(\"tiers\", {}).items():\n        tiers[tier] = TierPolicy(\n            can_customize=bool(cfg.get(\"can_customize\", False)),\n            max_cards=int(cfg.get(\"max_cards\", 6)),\n            allowed_cards=list(cfg.get(\"allowed_cards\", [])),\n        )\n    return tiers\n\ndef _allowed_set(policy: TierPolicy, registry_ids: Set[str]) -> Set[str]:\n    if \"*\" in policy.allowed_cards:\n        return set(registry_ids)\n    return set([c for c in policy.allowed_cards if c in registry_ids])\n\ndef resolve_cards_for_user(\n    user_tier: str,\n    policy: TierPolicy,\n    registry_meta: List[CardMeta],\n    selected_card_ids: List[str],\n) -> List[str]:\n    # Return ordered list of card_ids to include in the email.\n    registry_ids = set([m.id for m in registry_meta])\n    allowed = _allowed_set(policy, registry_ids)\n\n    if not policy.can_customize:\n        # Use defaults from meta, filtered by allowed.\n        metas = [m for m in registry_meta if m.id in allowed and m.default_enabled]\n        metas.sort(key=lambda m: (m.default_position, m.id))\n        return [m.id for m in metas[:policy.max_cards]]\n\n    # Customizable: use user's selection order, restricted by allowed and max_cards.\n    seen = set()\n    ordered = []\n    for cid in selected_card_ids:\n        if cid in allowed and cid not in seen:\n            ordered.append(cid)\n            seen.add(cid)\n        if len(ordered) >= policy.max_cards:\n            break\n\n    # If user selected nothing, fall back to defaults.\n    if not ordered:\n        metas = [m for m in registry_meta if m.id in allowed and m.default_enabled]\n        metas.sort(key=lambda m: (m.default_position, m.id))\n        ordered = [m.id for m in metas[:policy.max_cards]]\n\n    return ordered\n",
  "app/security.py": "from __future__ import annotations\n\nimport base64\nimport hashlib\nimport hmac\nimport json\nfrom typing import Any, Dict\n\nfrom .config import Settings\n\ndef _b64url(b: bytes) -> str:\n    return base64.urlsafe_b64encode(b).decode(\"ascii\").rstrip(\"=\")\n\ndef _unb64url(s: str) -> bytes:\n    pad = \"=\" * (-len(s) % 4)\n    return base64.urlsafe_b64decode(s + pad)\n\ndef _sign(settings: Settings, payload_b64: str) -> str:\n    mac = hmac.new(settings.secret_key.encode(\"utf-8\"), payload_b64.encode(\"utf-8\"), hashlib.sha256).digest()\n    return _b64url(mac)\n\ndef make_signed_token(settings: Settings, payload: Dict[str, Any]) -> str:\n    payload_b = json.dumps(payload, separators=(\",\", \":\"), sort_keys=True).encode(\"utf-8\")\n    payload_b64 = _b64url(payload_b)\n    sig = _sign(settings, payload_b64)\n    return f\"{payload_b64}.{sig}\"\n\ndef verify_signed_token(settings: Settings, token: str) -> Dict[str, Any]:\n    try:\n        payload_b64, sig = token.split(\".\", 1)\n    except ValueError:\n        raise ValueError(\"bad token\")\n\n    expected = _sign(settings, payload_b64)\n    if not hmac.compare_digest(expected, sig):\n        raise ValueError(\"bad signature\")\n\n    payload = json.loads(_unb64url(payload_b64).decode(\"utf-8\"))\n    return payload\n",
  "app/cards/__init__.py": "",
  "app/cards/base.py": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom datetime import date\nfrom typing import Protocol, Any\n\n@dataclass(frozen=True)\nclass CardMeta:\n    id: str\n    title: str\n    description: str\n    category: str = \"general\"\n    min_tier: str = \"FREE\"\n    default_enabled: bool = True\n    default_position: int = 100\n\nclass RenderContext(Protocol):\n    # Provide whatever your existing data pipeline exposes.\n    asof_date: date\n    public_base_url: str\n\n    def asset_url(self, path: str) -> str: ...\n    def data(self, key: str, default: Any = None) -> Any: ...\n\nclass Card(Protocol):\n    def meta(self) -> CardMeta: ...\n    def render_html(self, ctx: RenderContext) -> str: ...\n",
  "app/cards/registry.py": "from __future__ import annotations\n\nfrom typing import Dict, List\nfrom .base import Card, CardMeta\n\nclass CardRegistry:\n    def __init__(self) -> None:\n        self._cards: Dict[str, Card] = {}\n\n    def register(self, card: Card) -> None:\n        meta = card.meta()\n        if meta.id in self._cards:\n            raise ValueError(f\"Duplicate card id: {meta.id}\")\n        self._cards[meta.id] = card\n\n    def get(self, card_id: str) -> Card:\n        return self._cards[card_id]\n\n    def list_meta(self) -> List[CardMeta]:\n        return [c.meta() for c in self._cards.values()]\n\n    def ids(self) -> List[str]:\n        return list(self._cards.keys())\n\ndef registry_from_builtin() -> CardRegistry:\n    from .builtin.sp500_summary import Sp500SummaryCard\n    from .builtin.top_movers import TopMoversCard\n\n    reg = CardRegistry()\n    reg.register(Sp500SummaryCard())\n    reg.register(TopMoversCard())\n    return reg\n",
  "app/cards/builtin/__init__.py": "",
  "app/cards/builtin/_util.py": "from __future__ import annotations\nimport html\n\ndef escape(s: str) -> str:\n    return html.escape(s, quote=True)\n",
  "app/cards/builtin/sp500_summary.py": "from __future__ import annotations\nfrom ._util import escape\nfrom ..base import CardMeta, RenderContext\n\nclass Sp500SummaryCard:\n    def meta(self) -> CardMeta:\n        return CardMeta(\n            id=\"sp500_summary\",\n            title=\"S&P 500 Summary\",\n            description=\"Quick snapshot: close, daily change, trend.\",\n            category=\"index\",\n            min_tier=\"FREE\",\n            default_enabled=True,\n            default_position=10,\n        )\n\n    def render_html(self, ctx: RenderContext) -> str:\n        spx = ctx.data(\"spx\", {})\n        close = escape(str(spx.get(\"close\", \"n/a\")))\n        chg = escape(str(spx.get(\"chg\", \"n/a\")))\n        pct = escape(str(spx.get(\"pct\", \"n/a\")))\n        img = ctx.asset_url(f\"charts/{ctx.asof_date.isoformat()}/spx.png\")\n        return f\"\"\"\n        <section style=\"padding:14px;border:1px solid #e5e5e5;border-radius:12px;margin:12px 0;\">\n          <h2 style=\"margin:0 0 6px 0;font-size:18px;\">S&P 500</h2>\n          <div style=\"font-size:14px;margin-bottom:10px;\">\n            <b>Close:</b> {close} &nbsp; <b>\u0394:</b> {chg} ({pct})\n          </div>\n          <img src=\"{img}\" alt=\"S&P 500 chart\" style=\"width:100%;max-width:640px;border-radius:10px;\" />\n        </section>\n        \"\"\"\n",
  "app/cards/builtin/top_movers.py": "from __future__ import annotations\nfrom ._util import escape\nfrom ..base import CardMeta, RenderContext\n\nclass TopMoversCard:\n    def meta(self) -> CardMeta:\n        return CardMeta(\n            id=\"top_movers\",\n            title=\"Top Movers\",\n            description=\"Biggest gainers and losers.\",\n            category=\"market\",\n            min_tier=\"FREE\",\n            default_enabled=True,\n            default_position=20,\n        )\n\n    def render_html(self, ctx: RenderContext) -> str:\n        movers = ctx.data(\"top_movers\", [])\n        rows = []\n        for m in movers[:10]:\n            sym = escape(str(m.get(\"symbol\",\"\")))\n            pct = escape(str(m.get(\"pct\",\"\")))\n            rows.append(f\"<tr><td style='padding:4px 8px;'><b>{sym}</b></td><td style='padding:4px 8px;text-align:right;'>{pct}</td></tr>\")\n        table = \"<table style='width:100%;border-collapse:collapse;'>\" + \"\".join(rows) + \"</table>\"\n        return f\"\"\"\n        <section style=\"padding:14px;border:1px solid #e5e5e5;border-radius:12px;margin:12px 0;\">\n          <h2 style=\"margin:0 0 6px 0;font-size:18px;\">Top Movers</h2>\n          {table}\n        </section>\n        \"\"\"\n",
  "app/data/__init__.py": "",
  "app/data/provider.py": "from __future__ import annotations\n\nimport json\nimport os\nfrom dataclasses import dataclass\nfrom datetime import date\nfrom pathlib import Path\nfrom typing import Any, Dict\n\n@dataclass\nclass DataProvider:\n    data_dir: Path\n\n    def load_payload(self, asof: date) -> Dict[str, Any]:\n        # Contract: your existing pipeline should write a daily JSON bundle the cards can use.\n        # Example path: {data_dir}/bundles/2026-02-27.json\n        p = self.data_dir / \"bundles\" / f\"{asof.isoformat()}.json\"\n        if not p.exists():\n            # Empty payload is fine; cards should degrade gracefully.\n            return {}\n        return json.loads(p.read_text(encoding=\"utf-8\"))\n\ndef get_provider() -> DataProvider:\n    base = Path(os.getenv(\"DAILYSTONKS_DATA_DIR\", \"./sample_data\"))\n    return DataProvider(base)\n",
  "sample_data/bundles/2026-02-27.json": "{\n  \"spx\": {\n    \"close\": 5000.12,\n    \"chg\": \"+12.34\",\n    \"pct\": \"+0.25%\"\n  },\n  \"top_movers\": [\n    {\n      \"symbol\": \"AAPL\",\n      \"pct\": \"+3.2%\"\n    },\n    {\n      \"symbol\": \"TSLA\",\n      \"pct\": \"-2.1%\"\n    }\n  ]\n}\n",
  "app/delivery/__init__.py": "",
  "app/delivery/context.py": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom datetime import date\nfrom typing import Any, Dict\n\n@dataclass\nclass DeliveryContext:\n    asof_date: date\n    public_base_url: str\n    payload: Dict[str, Any]\n\n    def asset_url(self, path: str) -> str:\n        base = self.public_base_url.rstrip(\"/\")\n        path2 = path.lstrip(\"/\")\n        return f\"{base}/{path2}\"\n\n    def data(self, key: str, default: Any = None) -> Any:\n        return self.payload.get(key, default)\n",
  "app/delivery/template.py": "from __future__ import annotations\nfrom datetime import date\n\ndef wrap_email(subject: str, body_html: str, asof: date, unsubscribe_url: str) -> str:\n    # Conservative HTML for email clients.\n    return f\"\"\"<!doctype html>\n<html>\n  <head>\n    <meta charset=\"utf-8\"/>\n    <meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"/>\n    <title>{subject}</title>\n  </head>\n  <body style=\"font-family:Arial,Helvetica,sans-serif;background:#f7f7f7;margin:0;padding:0;\">\n    <div style=\"max-width:720px;margin:0 auto;padding:18px;\">\n      <header style=\"padding:10px 0 16px 0;\">\n        <div style=\"font-size:22px;font-weight:700;\">DailyStonks</div>\n        <div style=\"color:#666;font-size:13px;\">Market briefing for {asof.isoformat()}</div>\n      </header>\n      <main>\n        {body_html}\n      </main>\n      <footer style=\"margin-top:18px;color:#777;font-size:12px;line-height:1.4;\">\n        <p style=\"margin:0 0 6px 0;\">You are receiving this because you subscribed to DailyStonks.</p>\n        <p style=\"margin:0;\"><a href=\"{unsubscribe_url}\">Unsubscribe</a></p>\n      </footer>\n    </div>\n  </body>\n</html>\"\"\"\n",
  "app/delivery/mailer.py": "from __future__ import annotations\n\nimport smtplib\nfrom email.message import EmailMessage\nfrom email.utils import make_msgid\n\nfrom ..config import Settings\n\ndef send_email(\n    settings: Settings,\n    to_email: str,\n    subject: str,\n    html_body: str,\n    list_unsubscribe_url: str,\n) -> str:\n    msg = EmailMessage()\n    msg[\"Subject\"] = subject\n    msg[\"From\"] = settings.mail_from\n    msg[\"To\"] = to_email\n    msg[\"Message-ID\"] = make_msgid(domain=None)\n    msg[\"MIME-Version\"] = \"1.0\"\n    msg[\"List-Unsubscribe\"] = f\"<{list_unsubscribe_url}>\"\n    msg[\"List-Unsubscribe-Post\"] = \"List-Unsubscribe=One-Click\"\n\n    msg.set_content(\"Your email client does not support HTML.\")\n    msg.add_alternative(html_body, subtype=\"html\")\n\n    if settings.smtp_use_tls:\n        server = smtplib.SMTP(settings.smtp_host, settings.smtp_port, timeout=30)\n        server.starttls()\n    else:\n        server = smtplib.SMTP(settings.smtp_host, settings.smtp_port, timeout=30)\n\n    try:\n        if settings.smtp_username and settings.smtp_password:\n            server.login(settings.smtp_username, settings.smtp_password)\n        server.send_message(msg)\n    finally:\n        try:\n            server.quit()\n        except Exception:\n            pass\n\n    return msg[\"Message-ID\"]\n",
  "app/delivery/runner.py": "from __future__ import annotations\n\nfrom datetime import datetime, timedelta\nfrom zoneinfo import ZoneInfo\n\nfrom sqlalchemy import text\nfrom sqlalchemy.orm import joinedload\n\nfrom ..config import get_settings\nfrom ..db import session_scope\nfrom ..models import DeliverySchedule, DeliveryRun, CardSelection\nfrom ..cards.registry import registry_from_builtin\nfrom ..data.provider import get_provider\nfrom ..delivery.context import DeliveryContext\nfrom ..delivery.template import wrap_email\nfrom ..delivery.mailer import send_email\nfrom ..tier import load_tier_policies, resolve_cards_for_user\nfrom ..security import make_signed_token\n\ndef _parse_hhmm(hhmm: str) -> tuple[int, int]:\n    parts = hhmm.strip().split(\":\")\n    if len(parts) != 2:\n        raise ValueError(\"send_time_hhmm must be HH:MM\")\n    hh = int(parts[0]); mm = int(parts[1])\n    if not (0 <= hh <= 23 and 0 <= mm <= 59):\n        raise ValueError(\"invalid time\")\n    return hh, mm\n\ndef compute_next_run(schedule: DeliverySchedule, after_utc: datetime) -> datetime:\n    tz = ZoneInfo(schedule.timezone)\n    after_local = after_utc.replace(tzinfo=ZoneInfo(\"UTC\")).astimezone(tz)\n    hh, mm = _parse_hhmm(schedule.send_time_hhmm)\n    # Next run is the next occurrence of HH:MM local time strictly after \"after_local\".\n    candidate_local = after_local.replace(hour=hh, minute=mm, second=0, microsecond=0)\n    if candidate_local <= after_local:\n        candidate_local = candidate_local + timedelta(days=1)\n        candidate_local = candidate_local.replace(hour=hh, minute=mm, second=0, microsecond=0)\n    return candidate_local.astimezone(ZoneInfo(\"UTC\")).replace(tzinfo=None)\n\ndef run_due_deliveries(now_utc: datetime | None = None, limit: int = 20) -> int:\n    settings = get_settings()\n    now_utc = now_utc or datetime.utcnow()\n\n    reg = registry_from_builtin()\n    provider = get_provider()\n    tier_policies = load_tier_policies(settings)\n\n    processed = 0\n    with session_scope() as s:\n        # Lock due schedules to avoid double sending with multiple runners.\n        due_rows = s.execute(text(\"\"\"\n            SELECT id FROM delivery_schedules\n            WHERE enabled = TRUE AND next_run_at <= :now\n            ORDER BY next_run_at ASC\n            FOR UPDATE SKIP LOCKED\n            LIMIT :limit\n        \"\"\"), {\"now\": now_utc, \"limit\": limit}).fetchall()\n\n        schedule_ids = [r[0] for r in due_rows]\n        if not schedule_ids:\n            return 0\n\n        schedules = (\n            s.query(DeliverySchedule)\n            .options(joinedload(DeliverySchedule.user), joinedload(DeliverySchedule.target))\n            .filter(DeliverySchedule.id.in_(schedule_ids))\n            .all()\n        )\n\n        for sched in schedules:\n            user = sched.user\n            target = sched.target\n\n            # Skip unsubscribed targets.\n            if target.unsubscribed_at is not None or not user.is_active:\n                sched.next_run_at = compute_next_run(sched, now_utc)\n                s.add(sched)\n                processed += 1\n                continue\n\n            # Determine as-of date in user's timezone (so \"today\" matches their local day).\n            tz = ZoneInfo(sched.timezone)\n            now_local = now_utc.replace(tzinfo=ZoneInfo(\"UTC\")).astimezone(tz)\n            asof = now_local.date()\n\n            # Resolve card list for this user tier.\n            policy = tier_policies.get(user.tier) or tier_policies.get(\"FREE\")\n            selected = (\n                s.query(CardSelection)\n                .filter(CardSelection.user_id == user.id, CardSelection.enabled == True)\n                .order_by(CardSelection.position.asc(), CardSelection.created_at.asc())\n                .all()\n            )\n            selected_ids = [sel.card_id for sel in selected]\n            card_ids = resolve_cards_for_user(user.tier, policy, reg.list_meta(), selected_ids)\n\n            # Build render context payload from your daily bundle.\n            payload = provider.load_payload(asof)\n            ctx = DeliveryContext(asof_date=asof, public_base_url=settings.public_base_url, payload=payload)\n\n            # Render cards.\n            body_parts = []\n            for cid in card_ids:\n                try:\n                    card = reg.get(cid)\n                    body_parts.append(card.render_html(ctx))\n                except Exception:\n                    body_parts.append(f\"<section style='padding:14px;border:1px solid #e5e5e5;border-radius:12px;margin:12px 0;'><h2 style='margin:0 0 6px 0;font-size:18px;'>Card error: {cid}</h2><div style='color:#b00;font-size:13px;'>Failed to render.</div></section>\")\n            body_html = \"\\n\".join(body_parts)\n\n            unsub_token = make_signed_token(settings, {\"target_id\": str(target.id), \"ts\": int(now_utc.timestamp())})\n            unsub_url = f\"{settings.public_base_url.rstrip('/')}/unsubscribe/{unsub_token}\"\n            subject = f\"DailyStonks \u2022 {asof.isoformat()} \u2022 {user.tier}\"\n            full_html = wrap_email(subject, body_html, asof, unsub_url)\n\n            # Create run row first. Use now_utc truncated to minute for uniqueness.\n            run_at = now_utc.replace(second=0, microsecond=0)\n            run = DeliveryRun(schedule_id=sched.id, run_at=run_at, status=\"PENDING\")\n            s.add(run)\n            s.flush()\n\n            try:\n                msgid = send_email(settings, target.email, subject, full_html, unsub_url)\n                run.status = \"SENT\"\n                run.message_id = msgid\n            except Exception as e:\n                run.status = \"FAILED\"\n                run.error = str(e)[:2000]\n\n            # Update schedule next_run regardless, so we don't spam on failure.\n            sched.next_run_at = compute_next_run(sched, now_utc)\n            s.add(sched)\n            s.add(run)\n            processed += 1\n\n    return processed\n",
  "app/payments/__init__.py": "",
  "app/payments/paypal.py": "from __future__ import annotations\n\nimport json\nimport os\nfrom typing import Any, Dict, Optional\n\nimport httpx\n\nfrom ..config import Settings\n\ndef _env_json(name: str, default: str = \"{}\") -> Dict[str, Any]:\n    raw = os.getenv(name, default)\n    try:\n        return json.loads(raw)\n    except Exception:\n        return {}\n\ndef extract_customer_email(event: Dict[str, Any]) -> Optional[str]:\n    r = event.get(\"resource\") or {}\n    # Common locations for emails in PayPal events:\n    for path in [\n        (\"subscriber\", \"email_address\"),\n        (\"payer\", \"email_address\"),\n        (\"payer\", \"payer_info\", \"email\"),\n    ]:\n        cur: Any = r\n        ok = True\n        for k in path:\n            if isinstance(cur, dict) and k in cur:\n                cur = cur[k]\n            else:\n                ok = False\n                break\n        if ok and isinstance(cur, str) and \"@\" in cur:\n            return cur.strip().lower()\n    # Some events may put email at top-level.\n    email = event.get(\"email\") or event.get(\"payer_email\")\n    if isinstance(email, str) and \"@\" in email:\n        return email.strip().lower()\n    return None\n\ndef extract_plan_id(event: Dict[str, Any]) -> Optional[str]:\n    r = event.get(\"resource\") or {}\n    for k in [\"plan_id\", \"billing_plan_id\", \"planId\"]:\n        v = r.get(k)\n        if isinstance(v, str) and v:\n            return v\n    return None\n\ndef map_tier_from_plan_id(plan_id: Optional[str]) -> Optional[str]:\n    if not plan_id:\n        return None\n    mapping = _env_json(\"PAYPAL_PLAN_TIER_MAP\", \"{}\")  # {\"P-XXXX\":\"PRO\", ...}\n    tier = mapping.get(plan_id)\n    if isinstance(tier, str) and tier:\n        return tier.upper()\n    return None\n\ndef should_deactivate(event_type: str) -> bool:\n    et = (event_type or \"\").upper()\n    return any(x in et for x in [\"CANCEL\", \"SUSPEND\", \"EXPIRE\", \"TERMINAT\"])\n\nasync def get_access_token(settings: Settings) -> str:\n    if not settings.paypal_client_id or not settings.paypal_client_secret:\n        raise RuntimeError(\"Missing PAYPAL_CLIENT_ID/SECRET\")\n    auth = (settings.paypal_client_id, settings.paypal_client_secret)\n    async with httpx.AsyncClient(timeout=20) as client:\n        resp = await client.post(\n            f\"{settings.paypal_api_base.rstrip('/')}/v1/oauth2/token\",\n            data={\"grant_type\": \"client_credentials\"},\n            auth=auth,\n        )\n        resp.raise_for_status()\n        return resp.json()[\"access_token\"]\n\nasync def verify_webhook_signature(settings: Settings, headers: Dict[str, str], event: Dict[str, Any]) -> bool:\n    # If webhook verification isn't configured, accept (development mode).\n    if not settings.paypal_webhook_id or not settings.paypal_client_id or not settings.paypal_client_secret:\n        return True\n\n    # PayPal sends a set of headers we forward to verify endpoint.\n    # Header names can vary in casing. We normalize to lowercase before calling.\n    h = {k.lower(): v for k, v in headers.items()}\n    required = [\"paypal-transmission-id\", \"paypal-transmission-time\", \"paypal-cert-url\", \"paypal-auth-algo\", \"paypal-transmission-sig\"]\n    if any(k not in h for k in required):\n        return False\n\n    token = await get_access_token(settings)\n    payload = {\n        \"auth_algo\": h[\"paypal-auth-algo\"],\n        \"cert_url\": h[\"paypal-cert-url\"],\n        \"transmission_id\": h[\"paypal-transmission-id\"],\n        \"transmission_sig\": h[\"paypal-transmission-sig\"],\n        \"transmission_time\": h[\"paypal-transmission-time\"],\n        \"webhook_id\": settings.paypal_webhook_id,\n        \"webhook_event\": event,\n    }\n\n    async with httpx.AsyncClient(timeout=20) as client:\n        resp = await client.post(\n            f\"{settings.paypal_api_base.rstrip('/')}/v1/notifications/verify-webhook-signature\",\n            headers={\"Authorization\": f\"Bearer {token}\", \"Content-Type\": \"application/json\"},\n            json=payload,\n        )\n        resp.raise_for_status()\n        data = resp.json()\n        status = (data.get(\"verification_status\") or \"\").upper()\n        return status == \"SUCCESS\"\n",
  "app/main.py": "from __future__ import annotations\n\nimport json\nimport secrets\nimport os\nfrom datetime import datetime\n\nfrom fastapi import FastAPI, Request, HTTPException, Form\nfrom fastapi.responses import HTMLResponse, PlainTextResponse, RedirectResponse\nfrom pydantic import EmailStr\n\nfrom .config import get_settings\nfrom .db import session_scope\nfrom .models import User, DeliveryTarget, CardSelection, DeliverySchedule, PaymentEvent, Entitlement\nfrom .cards.registry import registry_from_builtin\nfrom .tier import load_tier_policies, resolve_cards_for_user\nfrom .security import verify_signed_token\nfrom .payments.paypal import (\n    extract_customer_email, extract_plan_id, map_tier_from_plan_id, should_deactivate, verify_webhook_signature\n)\nfrom .delivery.runner import compute_next_run\nfrom .delivery.runner import run_due_deliveries\n\nsettings = get_settings()\napp = FastAPI(title=\"DailyStonks Delivery Service\")\n\ndef _require_admin(request: Request) -> None:\n    admin = (request.headers.get(\"x-admin-token\") or \"\").strip()\n    expected = (os.getenv(\"ADMIN_TOKEN\") or settings.secret_key or \"\").strip()\n    if not expected or admin != expected:\n        raise HTTPException(status_code=401, detail=\"unauthorized\")\n\ndef _get_user_by_pref_token(s, token: str) -> User:\n    u = s.query(User).filter(User.preference_token == token, User.is_active == True).first()\n    if not u:\n        raise HTTPException(status_code=404, detail=\"user not found\")\n    return u\n\n@app.get(\"/health\", response_class=PlainTextResponse)\ndef health():\n    return \"ok\"\n\n@app.get(\"/cards\")\ndef list_cards():\n    reg = registry_from_builtin()\n    return {\"cards\": [c.__dict__ for c in reg.list_meta()]}\n\n@app.get(\"/prefs/{token}\", response_class=HTMLResponse)\ndef prefs_page(token: str):\n    reg = registry_from_builtin()\n    tier_policies = load_tier_policies(settings)\n\n    with session_scope() as s:\n        user = _get_user_by_pref_token(s, token)\n        policy = tier_policies.get(user.tier) or tier_policies.get(\"FREE\")\n        metas = reg.list_meta()\n        selections = s.query(CardSelection).filter(CardSelection.user_id == user.id).order_by(CardSelection.position.asc()).all()\n        selected_ids = [sel.card_id for sel in selections if sel.enabled]\n        resolved = resolve_cards_for_user(user.tier, policy, metas, selected_ids)\n        allowed = set([m.id for m in metas]) if \"*\" in policy.allowed_cards else set(policy.allowed_cards)\n\n        sched = s.query(DeliverySchedule).filter(DeliverySchedule.user_id == user.id).order_by(DeliverySchedule.created_at.asc()).first()\n        if not sched:\n            # Create default schedule + primary target if missing.\n            target = s.query(DeliveryTarget).filter(DeliveryTarget.user_id == user.id, DeliveryTarget.is_primary == True).first()\n            if not target:\n                target = DeliveryTarget(user_id=user.id, email=user.email, label=\"primary\", is_primary=True)\n                s.add(target); s.flush()\n            sched = DeliverySchedule(user_id=user.id, target_id=target.id, timezone=\"Europe/London\", send_time_hhmm=\"06:30\")\n            sched.next_run_at = compute_next_run(sched, datetime.utcnow())\n            s.add(sched); s.flush()\n\n        rows = []\n        for m in sorted(metas, key=lambda x: (x.default_position, x.id)):\n            is_allowed = (\"*\" in policy.allowed_cards) or (m.id in allowed)\n            checked = \"checked\" if m.id in resolved else \"\"\n            disabled = \"\" if (policy.can_customize and is_allowed) else \"disabled\"\n            rows.append(f\"\"\"<label style=\\\"display:block;padding:6px 0;opacity:{'1' if is_allowed else '0.4'}\\\">\\\n              <input type=\\\"checkbox\\\" name=\\\"card_id\\\" value=\\\"{m.id}\\\" {checked} {disabled}/>\\\n              <b>{m.title}</b> <span style=\\\"color:#666;font-size:12px;\\\">({m.id})</span><br/>\\\n              <span style=\\\"color:#444;font-size:13px;\\\">{m.description}</span>\\\n            </label>\"\"\")\n\n        customize_note = \"\" if policy.can_customize else \"<p><b>Note:</b> your tier uses a fixed set of cards. Upgrade to PRO to customize.</p>\"\n\n        html = f\"\"\"<!doctype html>\\\n<html><head><meta charset=\\\"utf-8\\\"/><meta name=\\\"viewport\\\" content=\\\"width=device-width,initial-scale=1\\\"/>\\\n<title>DailyStonks Preferences</title></head>\\\n<body style=\\\"font-family:Arial,Helvetica,sans-serif;background:#f7f7f7;margin:0;\\\">\\\n<div style=\\\"max-width:820px;margin:0 auto;padding:18px;\\\">\\\n  <h1 style=\\\"margin:0 0 6px 0;\\\">DailyStonks Preferences</h1>\\\n  <div style=\\\"color:#666;margin-bottom:18px;\\\">Account: <b>{user.email}</b> \u2022 Tier: <b>{user.tier}</b></div>\\\n  {customize_note}\\\n  <form method=\\\"post\\\" action=\\\"/prefs/{token}/save\\\" style=\\\"background:#fff;border:1px solid #e5e5e5;border-radius:12px;padding:14px;\\\">\\\n    <h2 style=\\\"margin:0 0 10px 0;font-size:18px;\\\">Cards</h2>\\\n    <p style=\\\"margin:0 0 10px 0;color:#666;font-size:13px;\\\">Max cards: {policy.max_cards}.</p>\\\n    {''.join(rows)}\\\n    <hr style=\\\"border:none;border-top:1px solid #eee;margin:14px 0;\\\"/>\\\n    <h2 style=\\\"margin:0 0 10px 0;font-size:18px;\\\">Schedule</h2>\\\n    <label>Timezone <input name=\\\"timezone\\\" value=\\\"{sched.timezone}\\\" style=\\\"width:240px;padding:6px;margin-left:8px;\\\"/></label><br/>\\\n    <label>Send time (HH:MM) <input name=\\\"send_time_hhmm\\\" value=\\\"{sched.send_time_hhmm}\\\" style=\\\"width:90px;padding:6px;margin-left:8px;\\\"/></label>\\\n    <div style=\\\"margin-top:12px;\\\">\\\n      <button type=\\\"submit\\\" style=\\\"padding:10px 14px;border-radius:10px;border:1px solid #111;background:#111;color:#fff;\\\">Save</button>\\\n      <a href=\\\"/prefs/{token}\\\" style=\\\"margin-left:10px;color:#333;\\\">Refresh</a>\\\n    </div>\\\n  </form>\\\n\\\n  <form method=\\\"post\\\" action=\\\"/prefs/{token}/send-test\\\" style=\\\"margin-top:14px;\\\">\\\n    <button type=\\\"submit\\\" style=\\\"padding:10px 14px;border-radius:10px;border:1px solid #555;background:#fff;\\\">Send test email now</button>\\\n  </form>\\\n\\\n</div>\\\n</body></html>\"\"\"\n        return HTMLResponse(html)\n\n@app.post(\"/prefs/{token}/save\")\ndef prefs_save(\n    token: str,\n    request: Request,\n    timezone: str = Form(...),\n    send_time_hhmm: str = Form(...),\n    card_id: list[str] = Form(default=[]),\n):\n    reg = registry_from_builtin()\n    tier_policies = load_tier_policies(settings)\n\n    with session_scope() as s:\n        user = _get_user_by_pref_token(s, token)\n        policy = tier_policies.get(user.tier) or tier_policies.get(\"FREE\")\n\n        sched = s.query(DeliverySchedule).filter(DeliverySchedule.user_id == user.id).order_by(DeliverySchedule.created_at.asc()).first()\n        if not sched:\n            target = s.query(DeliveryTarget).filter(DeliveryTarget.user_id == user.id, DeliveryTarget.is_primary == True).first()\n            if not target:\n                target = DeliveryTarget(user_id=user.id, email=user.email, label=\"primary\", is_primary=True)\n                s.add(target); s.flush()\n            sched = DeliverySchedule(user_id=user.id, target_id=target.id)\n            s.add(sched); s.flush()\n\n        sched.timezone = timezone.strip()\n        sched.send_time_hhmm = send_time_hhmm.strip()\n        sched.next_run_at = compute_next_run(sched, datetime.utcnow())\n        s.add(sched)\n\n        if policy.can_customize:\n            metas = reg.list_meta()\n            registry_ids = set([m.id for m in metas])\n            allowed = registry_ids if \"*\" in policy.allowed_cards else set(policy.allowed_cards) & registry_ids\n\n            chosen = [cid for cid in card_id if cid in allowed][:policy.max_cards]\n            s.query(CardSelection).filter(CardSelection.user_id == user.id).delete()\n            for i, cid in enumerate(chosen):\n                s.add(CardSelection(user_id=user.id, card_id=cid, enabled=True, position=i))\n\n        return RedirectResponse(url=f\"/prefs/{token}\", status_code=303)\n\n@app.post(\"/prefs/{token}/send-test\")\ndef send_test(token: str):\n    with session_scope() as s:\n        user = _get_user_by_pref_token(s, token)\n        sched = s.query(DeliverySchedule).filter(DeliverySchedule.user_id == user.id).first()\n        if not sched:\n            raise HTTPException(status_code=400, detail=\"no schedule\")\n        sched.next_run_at = datetime.utcnow()\n        s.add(sched)\n    run_due_deliveries(limit=1)\n    return RedirectResponse(url=f\"/prefs/{token}\", status_code=303)\n\n@app.get(\"/unsubscribe/{token}\", response_class=HTMLResponse)\ndef unsubscribe(token: str):\n    with session_scope() as s:\n        payload = verify_signed_token(settings, token)\n        target_id = payload.get(\"target_id\")\n        if not target_id:\n            raise HTTPException(status_code=400, detail=\"bad token\")\n        target = s.query(DeliveryTarget).filter(DeliveryTarget.id == target_id).first()\n        if not target:\n            raise HTTPException(status_code=404, detail=\"target not found\")\n        target.unsubscribed_at = datetime.utcnow()\n        s.add(target)\n\n    return HTMLResponse(\"\"\"<!doctype html><html><body style=\"font-family:Arial,Helvetica,sans-serif;\">\\\n    <h1>Unsubscribed</h1><p>You will no longer receive DailyStonks emails to this address.</p></body></html>\"\"\")\n\n@app.post(\"/webhooks/paypal\")\nasync def paypal_webhook(request: Request):\n    event = await request.json()\n    ok = await verify_webhook_signature(settings, dict(request.headers), event)\n    if not ok:\n        raise HTTPException(status_code=400, detail=\"invalid signature\")\n\n    event_id = str(event.get(\"id\") or \"\")\n    event_type = str(event.get(\"event_type\") or \"\")\n    if not event_id or not event_type:\n        raise HTTPException(status_code=400, detail=\"missing id/event_type\")\n\n    raw = json.dumps(event, separators=(\",\", \":\"), ensure_ascii=False)\n\n    with session_scope() as s:\n        existing = s.query(PaymentEvent).filter(PaymentEvent.event_id == event_id).first()\n        if existing:\n            return {\"ok\": True}\n\n        pe = PaymentEvent(event_id=event_id, event_type=event_type, raw_json=raw)\n        s.add(pe)\n        s.flush()\n\n        email = extract_customer_email(event)\n        plan_id = extract_plan_id(event)\n        new_tier = map_tier_from_plan_id(plan_id)\n\n        if email:\n            user = s.query(User).filter(User.email == email).first()\n            if not user:\n                user = User(email=email, tier=\"FREE\", is_active=True, preference_token=secrets.token_urlsafe(24))\n                s.add(user)\n                s.flush()\n                target = DeliveryTarget(user_id=user.id, email=email, label=\"primary\", is_primary=True)\n                s.add(target); s.flush()\n                sched = DeliverySchedule(user_id=user.id, target_id=target.id, timezone=\"Europe/London\", send_time_hhmm=\"06:30\")\n                sched.next_run_at = compute_next_run(sched, datetime.utcnow())\n                s.add(sched)\n\n            if should_deactivate(event_type):\n                user.tier = \"FREE\"\n                s.add(Entitlement(user_id=user.id, tier=user.tier, status=\"CANCELLED\"))\n            else:\n                if new_tier:\n                    user.tier = new_tier\n                    s.add(Entitlement(user_id=user.id, tier=new_tier, status=\"ACTIVE\"))\n            s.add(user)\n\n        pe.processed_at = datetime.utcnow()\n        s.add(pe)\n\n    return {\"ok\": True}\n\n@app.post(\"/admin/create-user\")\ndef admin_create_user(request: Request, email: EmailStr = Form(...), tier: str = Form(\"FREE\")):\n    _require_admin(request)\n    with session_scope() as s:\n        u = s.query(User).filter(User.email == str(email).lower()).first()\n        if not u:\n            u = User(email=str(email).lower(), tier=tier.upper(), is_active=True, preference_token=secrets.token_urlsafe(24))\n            s.add(u); s.flush()\n            t = DeliveryTarget(user_id=u.id, email=u.email, label=\"primary\", is_primary=True)\n            s.add(t); s.flush()\n            sched = DeliverySchedule(user_id=u.id, target_id=t.id, timezone=\"Europe/London\", send_time_hhmm=\"06:30\")\n            sched.next_run_at = compute_next_run(sched, datetime.utcnow())\n            s.add(sched)\n        link = f\"{settings.public_base_url.rstrip('/')}/prefs/{u.preference_token}\"\n        return {\"user_id\": str(u.id), \"prefs_link\": link}\n",
}

def main() -> None:
    args = [a for a in sys.argv[1:] if a]
    force = "--force" in args
    args2 = [a for a in args if not a.startswith("-")]
    target = Path(args2[0]) if args2 else Path("dailystonks-delivery")

    if target.exists() and any(target.iterdir()) and not force:
        print(f"[!] {target} exists and is not empty. Use --force to overwrite.")
        raise SystemExit(2)

    target.mkdir(parents=True, exist_ok=True)

    for rel, content in FILES.items():
        p = target / rel
        p.parent.mkdir(parents=True, exist_ok=True)
        p.write_text(content, encoding="utf-8")

    print(f"[✓] Wrote {len(FILES)} files into {target}")

if __name__ == "__main__":
    main()